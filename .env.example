# Optional application-side configuration. The core does not load this file.
# Construct your chosen provider client explicitly, then inject it.
AGENT_MODEL=
AGENT_BASE_URL=
AGENT_API_KEY=
# context_id and max_steps are explicit AgentRunner arguments.
AGENT_CONTEXT_ID=example
AGENT_MAX_STEPS=12
