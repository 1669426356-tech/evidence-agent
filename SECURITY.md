# 安全边界

本框架在宿主 Python 进程内执行已登记处理函数，不是代码沙箱。开发者负责工具的副作用声明、输入验证、文件与网络访问控制，以及外部操作的超时和幂等设计。

默认策略要求 `external_write` 操作具有精确绑定的一次性授权。将有副作用的工具错误声明为 `read_only` 会绕过这项要求。任意自然语言和历史经验均不构成授权。

授权消耗与外部操作没有跨系统事务；异常或超时之后，外部状态可能未知。不要根据本地失败记录自动认定远端没有执行，也不要重新创建同一许可来自动重试。

经验库中的目标和反思文本可能包含敏感信息。存储文件没有内置加密；接入远程聊天模型时，适配器会发送任务状态和历史建议。请由宿主应用实施数据选择和凭证管理。

框架不在一般错误记录中包含原始异常消息，但工具返回的正常数据、模型文本和调用方元数据不会自动脱敏。不要把凭证放入这些字段。

发布前需配置仓库的私密漏洞报告渠道。发现可能暴露凭证或允许未授权动作的问题时，不要把密钥或可识别的私有数据贴进公开 issue；通过发布者配置的私密渠道提供脱敏复现步骤。当前版本尚未承诺安全响应时限。
