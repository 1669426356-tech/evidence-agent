# 本地验证记录

日期：2026-09-25。验证对象为当前独立框架候选，Python 3.12.14 / Windows。

## 已完成

- 新建虚拟环境，通过 PyPI 安装本目录及测试依赖，未借用原工程的设备或数据。
- 57 项离线测试通过，包含授权绑定与消耗、排除字段和自定义序列化的指纹回归、证据失效、异常回退、状态隔离、SQLite、模型适配和快照验证。
- 离线示例完成 3 次决策、2 次工具计算和 2 条反思；最终目标值为 0，评价器判定通过。
- README 中的最小接入示例实际运行通过。
- 依赖一致性检查通过。
- 已验证源码分发包与 wheel 的构建，以及安装后的命令行示例。
- 独立目录通过发布脚本的范围、常见凭据格式和本机路径检查。

## 依赖版本

本次新环境使用 LangGraph 1.2.12、LangChain Core 1.6.5、Pydantic 2.13.5 和 pytest 8.4.2。完整版本见 `constraints-tested.txt`。此前也在较早的本地依赖版本上通过对应测试。

## 验证边界

- 没有调用真实模型服务、连接设备或执行原项目实验。
- 测试中的模型为脚本或替身，不能据此报告 LLM 成功率、领域性能或经验检索收益。
- GitHub CI 已配置 Windows/Linux、Python 3.11/3.12，远程 CI 尚未执行。
- 检查脚本不是完整安全审计，授权消费状态也不具备跨进程持久化语义。
- 仓库名为 `evidence-agent`，作者为 `hatetakename`，采用 MIT 许可证；当前版本尚未上传或发布。
