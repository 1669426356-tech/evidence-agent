# 贡献说明

本项目由 hatetakename 维护，采用 [MIT 许可证](LICENSE)。贡献中使用第三方内容时，请保留必要的来源和许可说明，并确认有权提交这些内容。

开发环境使用 Python 3.11+：

```bash
python -m pip install -e ".[test]"
python -m pytest -q
python -m agent_framework.demo
```

提交更改时说明触发条件、期望行为、实际行为及测试方法。测试应使用假工具、脚本模型或假聊天模型，不应依赖真实凭证、网络服务或设备。

新增工具应放在示例或独立集成项目中。核心框架保持领域无关；不要添加个人服务地址、机器路径、私有数据或设备默认配置。改变权限、证据失效或异常处理时，增加能够区分正确和错误行为的回归测试。

请勿提交密钥、令牌、`.env` 文件、经验数据库或含敏感内容的状态快照。涉及凭证的问题参见 [SECURITY.md](SECURITY.md)。
